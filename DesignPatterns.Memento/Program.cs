using System;

namespace DesignPatterns.Memento
{
    class Program
    {
        static void Main(string[] args)
        {
            // Tem a intenção de capturar e externalizar o estado interno de um objeto, sem violar o encapsulamento,
            // de modo que o mesmo possa posteriormente ser restaurado para esse estado

            // Permite salvar e restaurar o estado de um objeto sem quebrar as regras de encapsulamento definidas
            Console.WriteLine("Hello World!");
        }
    }
}
